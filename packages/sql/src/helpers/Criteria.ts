import { QueryCriteria } from '@typed-query-builder/builder';
import { DialectTransformTransformer } from '../Dialect';
import { DialectOutput } from '../Output';


export function getCriteria(criteria: QueryCriteria<any, any, any>, transform: DialectTransformTransformer, out: DialectOutput): string
{
  const {  } = criteria;

  let x = '';

  

  return x;
}